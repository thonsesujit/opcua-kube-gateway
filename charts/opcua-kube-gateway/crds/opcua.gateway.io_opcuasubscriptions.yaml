---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: opcuasubscriptions.opcua.gateway.io
spec:
  group: opcua.gateway.io
  names:
    kind: OPCUASubscription
    listKind: OPCUASubscriptionList
    plural: opcuasubscriptions
    singular: opcuasubscription
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OPCUASubscription is the Schema for the opcuasubscriptions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OPCUASubscriptionSpec defines the desired state of an OPCUASubscription.
            properties:
              endpoint:
                description: Endpoint is the OPC-UA server URL (e.g., "opc.tcp://plc:4840").
                minLength: 1
                type: string
              exporters:
                description: Exporters configures how data is exported.
                properties:
                  prometheus:
                    description: Prometheus configures the Prometheus metrics exporter.
                    properties:
                      enabled:
                        default: true
                        description: Enabled controls whether Prometheus metrics are
                          exported.
                        type: boolean
                      prefix:
                        default: opcua_
                        description: Prefix is the metric name prefix (default "opcua_").
                        type: string
                    type: object
                type: object
              nodes:
                description: Nodes is the list of OPC-UA nodes to subscribe to.
                items:
                  description: OPCUANode defines a single OPC-UA node to subscribe
                    to.
                  properties:
                    interval:
                      default: 5s
                      description: Interval is the subscription publishing interval
                        (default "5s").
                      type: string
                    name:
                      description: Name is the metric name for this node (lowercase,
                        underscores only).
                      pattern: ^[a-z][a-z0-9_]*$
                      type: string
                    nodeId:
                      description: NodeID is the OPC-UA node identifier (e.g., "ns=2;s=Temperature").
                      minLength: 1
                      type: string
                    unit:
                      description: Unit is the unit of measurement label (optional).
                      type: string
                  required:
                  - name
                  - nodeId
                  type: object
                minItems: 1
                type: array
              securityMode:
                default: None
                description: SecurityMode is the OPC-UA security mode (default "None").
                enum:
                - None
                - Sign
                - SignAndEncrypt
                type: string
            required:
            - endpoint
            - nodes
            type: object
          status:
            description: OPCUASubscriptionStatus defines the observed state of an
              OPCUASubscription.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastConnected:
                description: LastConnected is the timestamp of the last successful
                  connection.
                format: date-time
                type: string
              message:
                description: Message is a human-readable status message.
                type: string
              nodes:
                description: Nodes holds per-node subscription status.
                items:
                  description: NodeStatus holds the status of a single OPC-UA node
                    subscription.
                  properties:
                    error:
                      description: Error is the error message if the node subscription
                        failed.
                      type: string
                    lastUpdated:
                      description: LastUpdated is the timestamp of the last value
                        update.
                      format: date-time
                      type: string
                    lastValue:
                      description: LastValue is the last received value as a string.
                      type: string
                    nodeId:
                      description: NodeID is the OPC-UA node identifier.
                      type: string
                  required:
                  - nodeId
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
