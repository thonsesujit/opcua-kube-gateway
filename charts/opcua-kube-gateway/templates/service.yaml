apiVersion: v1
kind: Service
metadata:
  name: {{ include "opcua-kube-gateway.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opcua-kube-gateway.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "opcua-kube-gateway.selectorLabels" . | nindent 4 }}
